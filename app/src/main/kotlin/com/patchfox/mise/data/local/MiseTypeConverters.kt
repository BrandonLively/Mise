package com.patchfox.mise.data.local

/**
 * Placeholder — Room does not need any custom converters today since every entity column
 * is a primitive or String. Keeping the class so [MiseDatabase] keeps a stable reference;
 * methods may be added later if domain types start landing in Room directly.
 */
class MiseTypeConverters
